import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "ML Filter vs Baseline: Why Continuous Learning Matters on XAUUSD - SAIL Lab",
  description:
    "DeepAgent FX launch on MQL5 and GogoJungle. Controlled MT5 comparison: MODE_ML_TRADE vs MODE_DATASET_ONLY on XAUUSD M5. AI ensemble filter, continuous learning loop.",
};

export default function MLContinuousLearningLayout({ children }: { children: React.ReactNode }) {
  return children;
}
